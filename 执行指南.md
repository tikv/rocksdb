# ST-RocksDB 执行指南

> 企业级 CI/CD + Rust SDK 一站式解决方案

## 🎯 立即开始 (3步)

### 1. 提交到 GitHub (2分钟)

```bash
git add .
git commit -m "🚀 添加企业级CI/CD + Rust SDK"
git push origin denjixu_dev
```

### 2. 启用 GitHub Actions (1分钟)

进入 GitHub 仓库 → **Actions** → 点击启用

### 3. 验证运行 (2分钟)

```bash
echo "# CI/CD 测试成功 ✅" > CI_测试.md
git add . && git commit -m "test: 验证CI/CD" && git push
```

## 📋 核心功能

### 🤖 自动化 CI/CD
- **多平台构建**: Ubuntu + macOS
- **多编译器**: GCC + Clang
- **智能测试**: 并行执行
- **安全扫描**: Trivy + AddressSanitizer

### 🦀 Rust SDK
- **TiKV 优化**: 基于 TiKV 的性能改进
- **类型安全**: 完整的 Rust API
- **一键打包**: 自动化 SDK 生成
- **完整文档**: 中文使用指南

### 🛡️ 质量保障
- **PR 智能审查**: 自动生成中文测试报告
- **内存安全检查**: Valgrind + AddressSanitizer
- **性能监控**: 基准测试 + 回归检测

## 🚀 快速命令

### 本地开发
```bash
# 一键构建
./scripts/local_build_test.sh

# 生成 Rust SDK
./scripts/package_rust_sdk.sh 0.1.0

# Rust SDK 演示
./scripts/demo_rust_sdk.sh
```

### CI/CD 工作流

| 功能 | 文件 | 触发条件 |
|------|------|----------|
| 主 CI | `ci.yml` | Push/PR |
| PR 审查 | `pr-review.yml` | PR 创建/更新 |
| 格式检查 | `sanity_check.yml` | Push/PR |

### 测试矩阵

| 平台 | 编译器 | 构建类型 |
|------|--------|----------|
| Ubuntu | GCC/Clang | Debug/Release |
| macOS | 系统默认 | Debug/Release |

## 📦 文档结构

```
├── .github/workflows/      # CI/CD 配置
├── scripts/               # 自动化脚本
│   ├── local_build_test.sh     # 本地构建
│   ├── package_rust_sdk.sh     # Rust SDK 打包
│   └── demo_rust_sdk.sh        # Rust SDK 演示
├── RUST_SDK_GUIDE.md      # Rust SDK 使用指南
├── CI_CD_README.md        # CI/CD 技术文档
├── build_fix_guide.md     # 构建问题修复
└── 执行指南.md            # 本文件
```

## 🔧 高级使用

### 本地开发最佳实践
```bash
# 快速构建静态库
make static_lib DEBUG_LEVEL=0

# 并行构建
make -j$(nproc) static_lib

# 跳过测试快速验证
./scripts/local_build_test.sh --skip-tests
```

### Rust SDK 使用
```rust
use st_rocksdb::{DB, Options};

let mut opts = Options::default();
opts.create_if_missing(true);
let db = DB::open(&opts, "/path/to/db")?;

db.put(b"key", b"value")?;
let value = db.get(b"key")?.unwrap();
```

### PR 工作流程
1. 本地验证: `./scripts/local_build_test.sh`
2. 创建分支: `git checkout -b feature/xxx`
3. 提交代码: `git commit -m "feat: xxx"`
4. 推送分支: `git push origin feature/xxx`
5. 创建 PR → 自动生成中文测试报告

## 🛠️ 故障排除

### 构建失败
```bash
# 检查依赖
./scripts/local_build_test.sh

# 清理重建
make clean && make static_lib
```

### CI 失败
- **构建失败**: 查看 Actions 详细日志
- **测试失败**: 本地运行 `cargo test`
- **格式检查**: 运行 `make check-format`

## 📊 监控指标

### GitHub Actions 状态
- 进入仓库 → **Actions** 查看运行历史
- 查看 **Security** 页面的安全扫描结果
- PR 页面查看详细检查状态

### 分支保护建议
1. **Settings** → **Branches**
2. 保护 `main` 分支
3. 要求 PR 审查和状态检查通过

## 🎉 预期效果

部署后获得:

- **零手动干预**: 推送代码自动触发 CI/CD
- **并行测试**: 多任务同时运行，快速反馈
- **质量保障**: 构建+测试+安全+性能全覆盖
- **中文友好**: 完全中文化的错误提示和文档
- **Rust 生态**: 完整的 Rust SDK 支持

## 🆘 获取帮助

- 查看 `CI_CD_README.md` 了解技术细节
- 查看 `RUST_SDK_GUIDE.md` 学习 Rust SDK
- 查看 `build_fix_guide.md` 解决构建问题
- GitHub Issues 反馈问题 